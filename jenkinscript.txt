pipeline {
    agent any

    stages {
        stage('Git') {
            steps {
                git 'https://github.com/ramdevashram/pipe.git'
            }
        }
        stage('Maven') {
            steps {
                bat'mvn clean install'
            }
        }
    }
}

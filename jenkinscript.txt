pipeline {
    agent any

    stages {
        stage('Git') {
            steps {
                git branch: 'main', url: 'https://github.com/ramdevashram/pipe.git'
            }
        }
        stage('Maven') {
            steps {
                bat 'Maven install'
            }
        }
    }
}
